const bioPage = () => {
  const domString = `
  <div class="container align-self-center" id="bio-card">
  </div>`;
  document.querySelector('#bio-text').innerHTML = domString;
};

export default bioPage;
